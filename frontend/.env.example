# Set in Vercel → Project → Settings → Environment Variables (Production).
# Your Railway backend URL, no trailing slash, no /api suffix.
# Example: https://tejas-backend-production.up.railway.app
VITE_API_URL=
